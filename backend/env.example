# Server Configuration
PORT=3001
HTTP_PORT=80
HTTPS_PORT=443

# SSL/TLS Configuration
# Options: 'none', 'custom', 'letsencrypt'
SSL_MODE=none

# Custom Certificate Configuration (when SSL_MODE=custom)
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key
# SSL_CA_PATH=/path/to/ca-bundle.crt  # Optional

# Let's Encrypt Configuration (when SSL_MODE=letsencrypt)
# LETSENCRYPT_DOMAIN=example.com
# LETSENCRYPT_EMAIL=admin@example.com
# LETSENCRYPT_CERT_DIR=./certs
# LETSENCRYPT_ALT_NAMES=www.example.com,api.example.com  # Optional: comma-separated
# LETSENCRYPT_PRODUCTION=true  # Set to false for staging/testing
# SSL_REDIRECT_HTTP=true  # Redirect HTTP to HTTPS (default: true)

# Storage Configuration
# Options: 'file' or 'dynamodb'
STORAGE_TYPE=file

# File Storage Configuration (when STORAGE_TYPE=file)
DATA_DIR=./data

# DynamoDB Configuration (when STORAGE_TYPE=dynamodb)
# AWS_REGION=us-west-2
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_ENDPOINT=http://localhost:8000  # Optional: for local DynamoDB
# DYNAMODB_SERVICES_TABLE=purtal-services
# DYNAMODB_SETTINGS_TABLE=purtal-settings
# DYNAMODB_CLIENTS_TABLE=purtal-clients

# Encryption Configuration
# REQUIRED: Encryption key for SSH credentials (passwords, private keys, passphrases)
# Generate a secure random key: openssl rand -hex 32
# Or use a strong password (will be derived using PBKDF2)
# ENCRYPTION_KEY=your-secure-encryption-key-here